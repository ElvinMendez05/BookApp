
<div class="container my-5">
  <div class="row justify-content-center">
    <div class="col-md-10 col-lg-8">
      <div class="card shadow-sm border-0">
        <div class="row g-0 align-items-center">
          
          <!-- Texto a la izquierda -->
          <div class="col-md-7 p-4">
            <h2 class="fw-bold mb-4 text-primary">{{libro.nombre}}</h2>

            <p class="text-muted mb-2"><strong>Año de publicación:</strong> {{libro.anioPublicacion}}</p>
            <p class="text-muted mb-2"><strong>Categoría:</strong> {{libro.categoria.nombre}}</p>
            <p class="text-muted mb-2"><strong>Autor:</strong> {{libro.autor.nombre}}</p>
            <p class="text-muted mb-2"><strong>Editorial:</strong> {{ibro.editorial.nombre}}</p>
            <p class="text-muted mb-2"><strong>País de la editorial:</strong> {{libro.editorial.pais}}</p>
            <p class="text-muted mb-4"><strong>Teléfono de la editorial:</strong> {{libro.editorial.telefono}}</p>

            <a href="/" class="btn btn-outline-secondary">
              <i class="bi bi-arrow-left me-1"></i> Volver al listado
            </a>
          </div>

          <!-- Imagen a la derecha -->
          <div class="col-md-5 text-center p-3">
            <img 
              src="{{libro.imagen}}" 
              alt="{{libro.nombre}}" 
              class="img-fluid rounded shadow-sm"
              style="max-height: 320px; object-fit: cover; width: 100%;"
            />
          </div>

        </div>
      </div>
    </div>
  </div>
</div>
